# 残課題リスト

初回リリース後に見つかった課題を、業務への影響が大きい順に並べる。
着手するときは項目に「着手中」を付け、完了したら消して git 履歴に残す。

## このファイルの書き方(2つの作業が並行したとき、片方の記録が消えないように)

このファイルは複数の作業が同時に書き換えるため、2回続けて突き合わせが必要になった。
原因は「番号を連番で振ること」と「既存項目の説明文そのものを書き換えること」の2つだったので、
以降は次の3つを守る。

1. **新しい項目に連番を振らない。** 該当セクションの末尾に `### 新: タイトル — 判定` として足す。
   通し番号は棚卸しのときにまとめて振り直す(別々の作業が同じ番号を使う事故を防ぐ)。
2. **既存項目の説明文を書き換えず、その項目の下に日付付きで足す。**
   例: `- (2026-08-08 追記: 取込が失敗していた原因を直した)`。
   段落ごと書き直すと、同じ段落を触った作業同士が必ずぶつかる。
3. **項目を消すのは棚卸しのときだけ。** 消した理由は末尾の「消した項目」に必ず残す
   (何が終わったのかが後から分からなくなるため)。

検討して**採らなかった**手当てと理由:

- **項目ごとにファイルを分ける** — ぶつからなくなる代わりに、一覧を1枚で読めなくなる。
  このリストは依頼者への報告にそのまま添えるものなので、1枚であることの価値が上回る。
- **突き合わせを自動で「両方残す」設定にする(git の union マージ)** — このファイルは
  「終わった項目を消す」ことが通常運転なので、消した行が自動で復活してしまう。害のほうが大きい。
- **課題管理をGitHubのIssueに移す** — 依頼者が読む場所が増える。いまは1ファイルで足りている。

**2026-08-08 に全項目を棚卸しした。** 各項目に次のいずれかの判定を付けている。

- **実装する** — 順番待ち。いつ着手するかだけの問題。
- **待つ** — 着手条件が満たされるまで動かさない。条件を各項目に書く。
- **確認待ち** — 業務側に事実を聞かないと作れない。聞く相手と内容を書く。
- **完了・削除** — 実装済みまたは不要になったもの(この版で本文から消した。下の「この棚卸しで消した項目」に理由を残す)。

## 高 (業務が止まる・数字が間違う)

### 1. 車両マスタ・運転者マスタが本番で0件 — 実装する

本番D1の `vehicle_master` と `driver_master` が0件のまま。手入力画面の車両一覧も収支表の計算も
車両マスタを起点にしているため、これが埋まるまで収支表は1行も作れない。
2026-05 の `vehicle_pl` 106件は「完成済み収支表Excel」を写したもので、CSVから計算した結果ではない。

→ 管理画面から車両マスタ・運転者マスタを取り込む運用手順を確立する。
   **本番公開の前提条件**。3-3 と合わせて、公開前に必ず片付ける。

**取り込む仕組みの側は 2026-08-08 に完成した**(残っているのは本番データを実際に入れる運用のみ)。

- 0件のときは画面が理由と登録導線を出す。
- 社内Excelをそのまま取り込めるようにし、失敗したときは理由を画面に出すようにした。
- 取込が必ず失敗していた原因を修正した。画面の既定の対象月が当月−2、渡されるExcelは前月分までしか
  無いことがあり、月が合わないと 422 で止まっていた。マスタは月ごとの実績ではないため、
  いちばん新しい月のシートで代用し、どのシートを読んだかを画面に出すようにした。
  仕様は `docs/product/master-data-spec.md`、画面から通ることは `tests/e2e/master-import.spec.ts` で固定済み。

→ **残っている作業**: 本番の `vehicle_master` / `driver_master` に実際のマスタを取り込み、
   2 の古い106行の掃除と合わせて数字を作り直すこと。仕組みではなく運用の手順の問題になった。

### 2. 収支表を作り直しても古い行が残る — 実装する

`D1VehiclePlRepository.upsertMany` は対象が0行のとき何もせずに戻り、`removeVehicles` も
今回の対象から外れた車両しか消さない。そのため車両マスタが0件でも「作り直す」が成功を返し、
旧Excel由来の106行がそのまま残る。画面側では0台のとき成功として見せないようにしたが、
データ側の掃除は未対応。**1 と同時に直す**(1を片付けた瞬間に、古い106行と新しい行が混ざるため)。

### 3. 進捗表示の分母が実データと矛盾する — 実装する

`getWorkflowProgress` の分母が `vehicle_pl` の件数なので「表は0行なのに106台中0台」と出る。
分母を車両マスタの台数に変える。1・2 と同じ回で直すのが安全。

### 3-3. 本番の Google ログイン設定一式 — 待つ(依頼者の Google アカウント情報が要る)

`docs/product/T4-release-readiness.md` の必須ブロッカー1〜5を参照。
この設定が入るまで本番公開は保留する。着手条件: 依頼者から本番用の Google Cloud プロジェクトと
許可メールアドレスの一覧をもらうこと。

## 中 (手間が増える・間違えやすい)

### 4. 車両マスタに無い車番を仮登録できない — 待つ(1のCSV取込を入れてから)

運行実績CSVには車番が出ているのに車両マスタに無い車両は、手入力画面に行が出ない。
「実績に出ている車番をワンクリックで仮登録する」導線があれば、マスタ整備を待たずに進められる。
(いま画面は「N台の車番が記録されています」と件数を出すところまで)
着手条件: 1 のCSV取込を入れてもなお、マスタに無い車番が毎月出るなら作る。
CSV取込で足りるなら仮登録は作らない(入口が2つあるとマスタが荒れる)。

### 5. 手入力の保存が全車両を上書きする — 待つ(2人以上で分担入力を始めたら)

保存時に画面上の全車両分を送るため、2人で分担して入力すると後から保存した側の値で
相手の入力が巻き戻る可能性がある。触った車両だけを送る差分保存にしたい。
着手条件: 実際に2人以上が同じ月を同時に入力する運用になったとき。
1人で入力している限りは実害が出ない(`ManualEntryStepper.tsx` の保存は全件送信のまま)。

### 6. 修繕費だけフォールバックが非対称 — 確認待ち(質問票を渡した。回答待ち)

タイヤ代と高速料金は未入力なら自動計算に回るが、修繕費は空欄=0円になる。
`repairStandard`(走行距離×単価)を計算しているのに使っていない
(`vehiclePlCalculation.ts` に計算だけある)。
聞くことと、回答ごとの対応は `docs/product/業務確認事項.md` 質問2 に記載。
2026-08-08 時点でできているのは「確定の直前に未入力の台数を出す」ところまで。
**保存後は0円と未入力が同じ数字になり区別が付かない**(`manual_vehicle_input.repair` が 0 固定のため)。
区別できるようにするには保管の作り変えが要るので、回答が「入れ忘れがありうる」だったときに着手する。

### 7. 「その他諸経費」に入力欄が無い — 確認待ち(質問票を渡した。回答待ち)

`miscOther` は収支表の列としても計算経路としても存在するが、入力する画面が無い。
旧Excelから取り込んだ値を毎月持ち回しているだけ。業務フロー docx にも対応する手順が無い。
聞くことと、回答ごとの対応は `docs/product/業務確認事項.md` 質問3 に記載。
確認して確定のステップに「入力欄が無い項目」として明示するところまでは実装済み。

### 12. デジタコの総給油量にインタンク以外が含まれるか未確認 — 確認待ち(質問票を渡した。回答待ち)

聞くことと、回答ごとの対応は `docs/product/業務確認事項.md` 質問1 に記載(**3件のうち最優先**)。
調べた結果、デジタコの総給油量(`vehicleOperationParser.totalFuelQtyLiter`)は取り込んでいるだけで
**どの計算にも使っていない**ことが分かった。そのため、どちらの回答でも今日の金額は動かない。
影響が出るのは「インタンク給油量の側に外部給油分が含まれていた場合」で、燃費(km/ℓ)が実際より悪く出る。
いまの前提は燃料費のステップの画面と `vehiclePlCalculation.ts` に書き出してある。

### 24. マスタから車両・運転者を外す手段が無い — 待つ(外したい要望が出たら)

両マスタとも取込は追加と上書きだけで、既存の行を消さない(`docs/product/master-data-spec.md` §4)。
売却した車両・退職した運転者を一覧から外したい要望が出たら、削除ではなく無効化フラグとして作る。
いまは車番未割当や原価区分で見分ける運用。
着手条件: 実際に「もう使っていない車両が一覧に出て邪魔だ」と言われたとき。
過去の収支表がその車両を参照しているため、行そのものは消さない(残課題リスト §8 の方針)。

### 新: 運行実績CSVが本社分しか手元に無い — 確認待ち(他営業所の実ファイル待ち)

業務フロー docx は「3営業所分すべて取り込む」と書いているが、共有された実データは本社分1ファイルのみ。
津山ほかのファイル名と列構成は未確認で、列は本社と同じ前提で実装している。
実ファイルをもらって確かめる。(`docs/product/data-flow-map.md` §7)

### 新: 燃料費・修繕費・高速料金の原票が自動で読めない — 待つ(原票を入手したら)

STEP3・5・6 の元データは紙・PDFの請求書と給油機レシートで、CSVが存在しない。
現状は手入力画面が正規の経路。OCRでの自動読取は原票を入手してから検討する。

### 新: 給与集計表・運行実績表の年月は毎回人が選ぶ — 確認待ち(日付列を追加できるか確認する)

この2つの帳票は中身に日付が1つも無く(給与59列・運行14列を全数確認)、何年何月分かを自動で決められない。
取込のたびに画面で選んでもらう形にしてある。出力元システムの設定で日付列を足せるなら、
それが根本解決になる。(`docs/product/data-flow-map.md` §6-1)

### 25. CSVがUTF-8だと文字化けして取り込めない — 待つ(UTF-8のCSVを渡される場面が出たら)

すべてのCSV経路が cp932 前提で読む。UTF-8で書き出したCSVを渡すと、見出しが文字化けした
エラーメッセージ(「遉ｾ蜩｡No」等)になり、原因が読み取れない。
Excelの直接取込を主経路にしたため優先度は下がったが、文字コードの自動判別を入れたい。
着手条件: Excelではなく UTF-8 のCSVを渡す運用が実際に出たとき。
なお取込前の下読み(2026-08-08 追加)も cp932 前提で見出しを読むため、UTF-8 のCSVでは
「必要な列が見つかりませんでした」と出る。取り込めない事実は同じだが、理由の案内は文字コードに
言及した方が親切。文字コード自動判別と同時に直す。

### 新: 取り込んだファイルの記録が増え続ける — 待つ(1年運用してから)

`file_import_log` に取込1回ごとに1行たまる。月20回として年240行で、当面は表示上限(200件)で足りる。
古い記録の自動削除や年月での絞り込みは、実際に見づらくなってから入れる。
着手条件: 「取込データ管理」の記録一覧が長すぎて目的の行を探せない、と言われたとき。

## 低 (あると嬉しい)

### 9. 画面の網羅的な自動テスト(Playwright) — 待つ(主要フローが固まってから)

いまは `tests/e2e/auth-gate.spec.ts` と `admin.spec.ts` の2本のみ。
請求書を上から入力して確定するまでと、収支表の確認画面を1件ずつ処理して反映するまでを、
それぞれ1本通しておきたい。
着手条件: 依頼者が2〜3ヶ月分を実際に回し、画面の作りがこれ以上変わらないと決まったとき。
いま固定すると、ブラッシュアップのたびにテストを書き直すことになる。

### 13. 集中モードの中での並べ替え・絞り込み — 待つ(要望が出たら)

「後回しにした指摘だけを見る」絞り込みは 2026-08-08 に実装した(収支表の
「後回しの◯件だけ確認する」)。残りは車両ごと・指摘の種類ごとのまとめ表示。
着手条件: 「同じ車両の指摘をまとめて見たい」と実際に言われたとき。
いまは「要修正 → 要確認 → 参考」の順に1件ずつ通す運用で足りている。

### 15. 確認画面から元の伝票を1枚ずつ見る — 待つ(取込画面に手を入れるとき)

いまは元になっている数字(合計)まで表示している。伝票そのものの表示は
取込画面の作りに手を入れる必要があるため、取込まわりを触る回にまとめて行う。

### 17. セル単位の直しの履歴画面 — 待つ(「確認の記録」で足りるか見てから)

誰がいつ何を直したかは操作の履歴に残っており追える。加えて 2026-08-08 に
「確認の記録」(`/grid/report`)で、直した数字と判断を月単位で1枚に出せるようにした。
着手条件: 月単位ではなく「この車両のこの半年」を追いたい場面が出たとき。

### 18. 複数セルのコピー&貼り付け・範囲選択 — 待つ(要望が出たら)

1件ずつ直しながら判断する作業なので、まとめて貼る場面がまだ無い。

### 19. 乗務員数・賞与(月額)を収支表の上で直す — 待つ(要望が出たら)

収支表に列が無い項目のため、従来どおり車両ごとの詳細画面から直す。

### 20. 表の行の仮想スクロール(見えている行だけ描く) — 待つ(台数が増えたら)

106台では実測で問題が出ていない。着手条件: 台数が200台を超える、
または表の表示が体感で重くなったとき。

### 21. 確認済みにするときのメモ入力欄 — 待つ(判断理由を残したい要望が出たら)

保存の仕組みは用意済み(APIも保存先も `note` を受け取れる)。
「確認の記録」も、メモがあれば判断の横に出すように作ってある。
いま足りていないのは画面の入力欄だけ。
着手条件: 「なぜOKにしたのか後から分からない」と実際に言われたとき。
先回りで欄を増やすと、毎回の確認が1手増える(106台×毎月に効く)ので今は出さない。

### 22. AI分析の有効化 — 待つ(依頼者がAPIキーを用意したら)

画面と処理は実装済みで、APIキーを設定すれば動く。
着手条件: 収支表の確認作業が固まり、依頼者が費用込みで使うと決めたとき
(使う前に月額の見込みを出して判断してもらう)。

### 23. 本番URLでの Core Web Vitals 計測 — 待つ(本番公開後)

本番公開後、実際のURLで測る。3-3 が片付くまで測れない。

### 26. 後回しが長く残ったときの気づき — 待つ(1〜2ヶ月運用してから)

「あとで見る」にした指摘は翌月に引き継がない(判断の先送りを自動で持ち越すと誰も見なくなるため)。
そのぶん、月をまたぐ前に片付ける必要がある。いまは件数を画面に出すところまで。
着手条件: 実際に後回しのまま月が締まる事例が出たとき。そのとき初めて通知や締めの制限を考える。

## 作らない判断をしたもの

### 収支表の差分再計算 — 作らない

キリン協力金の按分やトレーラの吸収で1台の直しが他の行に波及するため、
差分計算は表全体と同じ知識が必要になる。作り直しを呼ぶ回数を1回に減らすことで
待ち時間の問題は解けているので、作らない。

## この棚卸しで消した項目 (2026-08-08)

- **14. 同じ種類の指摘をまとめて「このままでOK」にする** — 実装済みのため削除。
  集中モードのカードに「同じ指摘 ◯件 をまとめてOK」を出し、対象を全件並べた確認を挟んでから実行、
  実行後は「元に戻す」を出す。要修正(赤)には出さない。判断の経緯は
  `docs/product/T5-grid-inline-review.md` §6.2。
- **16. 表の上でセルを直接直す使い方の案内** — 実装済みのため削除。
  表モードに入ったときだけ操作説明を出し、最初の導線には出さない形で決着している
  (`GridTable.tsx`)。同 §5.3 の方針どおり。

## この回で消した項目 (2026-08-08 追加分・毎月の入力を軽くする)

- **3-2. インタンク単価が毎月0円から始まる** — 実装済みのため削除。
  今月の単価が未設定なら前月の単価を入れた状態で開く。黙って埋めず「先月(YYYY-MM)の単価を
  そのまま入れています」と出し、「確認しました」を押すまで印が消えない。前月も記録が無ければ
  0のままにして「単価が0のままだと全車の軽油代が0円になります」を出す。
- **8. 前月の値をコピーして始める** — 実装済みのため削除。
  燃料4項目と修繕費に「空欄に先月(YYYY-MM)の値を入れる ◯件」を用意した。
  **入っている値は上書きしない**(今月の請求書から写した数字を潰さない)。入れた欄には「先月の値」の印が付き、
  「元に戻す」で1操作で戻せる。空欄のセルには押す前から「先月 ◯◯」を出す。
  コピー元が無い月(初月・データ欠損月)はボタン自体を出さない。
- **10. 手入力の保存で確定が黙って解除される** — 実装済みのため削除。
  確定済みの月は画面の上部に「この月は確定済みです」を常時出し、保存を押した時点では何もせず
  「確定を解除して保存しますか?」を1回だけ確認する(やめれば入力に戻れる)。確認は1回だけ。
- **11. 入力中に年月を切り替えると入力が消える** — 実装済みのため削除。
  入力のたびに下書きを自動保存し(手が止まってから 800ms)、次に開いたときに
  「◯月◯日 ◯時◯分 時点の入力途中を読み込みました」と出して復元する。「下書きを破棄して
  保存済みの状態に戻す」で捨てられる。保存に成功したら下書きは消す。
  **確定済みの月では下書きを保存も復元もしない**(締めた月に身に覚えのない数字が復活しないように)。
